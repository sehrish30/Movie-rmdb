import React from 'react'
import {StyledSpinner} from '../styles/StyledSpinner'

const Spinner = () => {
    return (
        <div>
            <StyledSpinner />
        </div>
    )
}

export default Spinner
